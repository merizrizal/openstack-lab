---
- name: Define fixed approval-scoped MCP transfer paths
  ansible.builtin.set_fact:
    ai_ops_mcp_wheelhouse_transfer_source: >-
      {{ ai_ops_mcp_wheelhouse_transfer.builder.publication_root }}/{{ ai_ops_mcp_wheelhouse_transfer.approval.id }}
    ai_ops_mcp_wheelhouse_transfer_controller_stage: >-
      {{ ai_ops_mcp_wheelhouse_transfer.controller.staging_root }}/{{ ai_ops_mcp_wheelhouse_transfer.approval.id }}
    ai_ops_mcp_wheelhouse_transfer_assistant_stage: >-
      {{ ai_ops_mcp_wheelhouse_transfer.assistant.staging_root }}/{{ ai_ops_mcp_wheelhouse_transfer.approval.id }}
    ai_ops_mcp_wheelhouse_transfer_destination: >-
      {{ ai_ops_mcp_wheelhouse_transfer.assistant.publication_root }}/{{ ai_ops_mcp_wheelhouse_transfer.approval.id }}

- name: Inspect selected builder MCP wheelhouse publication
  ansible.builtin.stat:
    path: "{{ ai_ops_mcp_wheelhouse_transfer_source }}"
    follow: false
  delegate_to: "{{ ai_ops_mcp_wheelhouse_transfer.builder.host }}"
  become: true
  register: ai_ops_mcp_wheelhouse_transfer_source_stat
  changed_when: false

- name: Assert selected builder MCP wheelhouse boundary
  ansible.builtin.assert:
    that:
      - ai_ops_mcp_wheelhouse_transfer_source_stat.stat.exists
      - ai_ops_mcp_wheelhouse_transfer_source_stat.stat.isdir
      - not ai_ops_mcp_wheelhouse_transfer_source_stat.stat.islnk
      - ai_ops_mcp_wheelhouse_transfer_source_stat.stat.pw_name == 'root'
      - ai_ops_mcp_wheelhouse_transfer_source_stat.stat.gr_name == 'root'
      - ai_ops_mcp_wheelhouse_transfer_source_stat.stat.mode == '0550'
    fail_msg: ERR_MCP_WHEELHOUSE_TRANSFER_SOURCE_BOUNDARY

- name: Discover selected builder MCP wheelhouse regular files
  ansible.builtin.find:
    paths: "{{ ai_ops_mcp_wheelhouse_transfer_source }}"
    file_type: file
    recurse: true
  delegate_to: "{{ ai_ops_mcp_wheelhouse_transfer.builder.host }}"
  become: true
  register: ai_ops_mcp_wheelhouse_transfer_source_files
  changed_when: false

- name: Discover selected builder MCP wheelhouse symlinks
  ansible.builtin.find:
    paths: "{{ ai_ops_mcp_wheelhouse_transfer_source }}"
    file_type: link
    recurse: true
  delegate_to: "{{ ai_ops_mcp_wheelhouse_transfer.builder.host }}"
  become: true
  register: ai_ops_mcp_wheelhouse_transfer_source_links
  changed_when: false

- name: Define selected builder MCP wheelhouse relative paths
  ansible.builtin.set_fact:
    ai_ops_mcp_wheelhouse_transfer_relative_paths: >-
      {{
        ai_ops_mcp_wheelhouse_transfer_source_files.files |
        map(attribute='path') |
        map('regex_replace', '^' ~ (ai_ops_mcp_wheelhouse_transfer_source | regex_escape) ~ '/', '') |
        list
      }}

- name: Assert selected builder MCP wheelhouse contains only reviewed artifacts
  ansible.builtin.assert:
    that:
      - ai_ops_mcp_wheelhouse_transfer_source_links.matched | int == 0
      - ai_ops_mcp_wheelhouse_transfer_source_files.matched | int > 3
      - "'requirements.lock' in ai_ops_mcp_wheelhouse_transfer_relative_paths"
      - "'manifest.json' in ai_ops_mcp_wheelhouse_transfer_relative_paths"
      - "'manifest.sha256' in ai_ops_mcp_wheelhouse_transfer_relative_paths"
      - >-
        ai_ops_mcp_wheelhouse_transfer_source_files.files |
        map(attribute='path') | map('basename') | list | unique | length ==
        ai_ops_mcp_wheelhouse_transfer_source_files.matched | int
      - >-
        ai_ops_mcp_wheelhouse_transfer_relative_paths |
        reject('match', '^(requirements\\.lock|manifest\\.json|manifest\\.sha256|wheels/[^/]+\\.whl)$') |
        list | length == 0
    fail_msg: ERR_MCP_WHEELHOUSE_TRANSFER_SOURCE_CONTENTS

- name: Inspect selected builder MCP wheelhouse file boundaries
  ansible.builtin.stat:
    path: "{{ item.path }}"
    follow: false
  loop: "{{ ai_ops_mcp_wheelhouse_transfer_source_files.files }}"
  delegate_to: "{{ ai_ops_mcp_wheelhouse_transfer.builder.host }}"
  become: true
  register: ai_ops_mcp_wheelhouse_transfer_source_file_stats
  changed_when: false

- name: Assert selected builder MCP wheelhouse file boundaries
  ansible.builtin.assert:
    that:
      - item.stat.exists
      - item.stat.isreg
      - not item.stat.islnk
      - item.stat.pw_name == 'root'
      - item.stat.gr_name == 'root'
      - item.stat.mode == '0440'
    fail_msg: ERR_MCP_WHEELHOUSE_TRANSFER_SOURCE_BOUNDARY
  loop: "{{ ai_ops_mcp_wheelhouse_transfer_source_file_stats.results }}"
  loop_control:
    label: source-artifact

- name: Verify selected builder MCP wheelhouse manifest
  ansible.builtin.command:
    argv: [/usr/bin/sha256sum, --check, --strict, --status, manifest.sha256]
    chdir: "{{ ai_ops_mcp_wheelhouse_transfer_source }}"
  delegate_to: "{{ ai_ops_mcp_wheelhouse_transfer.builder.host }}"
  become: true
  changed_when: false
  no_log: true

- name: Inspect selected assistant MCP wheelhouse destination
  ansible.builtin.stat:
    path: "{{ ai_ops_mcp_wheelhouse_transfer_destination }}"
    follow: false
  register: ai_ops_mcp_wheelhouse_transfer_destination_stat
  changed_when: false

- name: Reject replacement of an assistant MCP wheelhouse publication
  ansible.builtin.assert:
    that:
      - not ai_ops_mcp_wheelhouse_transfer_destination_stat.stat.exists
    fail_msg: ERR_MCP_WHEELHOUSE_TRANSFER_DESTINATION_EXISTS

- name: Transfer and publish selected MCP wheelhouse
  block:
    - name: Remove stale controller MCP wheelhouse staging path
      ansible.builtin.file:
        path: "{{ ai_ops_mcp_wheelhouse_transfer_controller_stage }}"
        state: absent
      delegate_to: localhost
      become: false

    - name: Create user-owned controller MCP wheelhouse staging path
      ansible.builtin.file:
        path: "{{ ai_ops_mcp_wheelhouse_transfer_controller_stage }}"
        state: directory
        mode: "0700"
      delegate_to: localhost
      become: false

    - name: Fetch selected builder MCP wheelhouse files to controller staging
      ansible.builtin.fetch:
        src: "{{ item.path }}"
        dest: "{{ ai_ops_mcp_wheelhouse_transfer_controller_stage }}/"
        flat: true
      loop: "{{ ai_ops_mcp_wheelhouse_transfer_source_files.files }}"
      delegate_to: "{{ ai_ops_mcp_wheelhouse_transfer.builder.host }}"
      become: true
      no_log: true

    - name: Create root-owned assistant MCP wheelhouse publication roots
      ansible.builtin.file:
        path: "{{ item.path }}"
        state: directory
        owner: root
        group: root
        mode: "{{ item.mode }}"
      loop:
        - path: "{{ ai_ops_mcp_wheelhouse_transfer.assistant.publication_root }}"
          mode: "0750"
        - path: "{{ ai_ops_mcp_wheelhouse_transfer.assistant.staging_root }}"
          mode: "0700"
        - path: "{{ ai_ops_mcp_wheelhouse_transfer_assistant_stage }}"
          mode: "0700"
        - path: "{{ ai_ops_mcp_wheelhouse_transfer_assistant_stage }}/wheels"
          mode: "0700"

    - name: Copy selected builder MCP wheelhouse files to assistant staging
      ansible.builtin.copy:
        src: "{{ ai_ops_mcp_wheelhouse_transfer_controller_stage }}/{{ item.path | basename }}"
        dest: >-
          {{ ai_ops_mcp_wheelhouse_transfer_assistant_stage }}/{{
          item.path | regex_replace('^' ~ (ai_ops_mcp_wheelhouse_transfer_source | regex_escape) ~ '/', '') }}
        owner: root
        group: root
        mode: "0440"
      loop: "{{ ai_ops_mcp_wheelhouse_transfer_source_files.files }}"
      no_log: true

    - name: Verify copied assistant MCP wheelhouse manifest
      ansible.builtin.command:
        argv: [/usr/bin/sha256sum, --check, --strict, --status, manifest.sha256]
        chdir: "{{ ai_ops_mcp_wheelhouse_transfer_assistant_stage }}"
      changed_when: false
      no_log: true

    - name: Seal copied assistant MCP wheel directory
      ansible.builtin.file:
        path: "{{ ai_ops_mcp_wheelhouse_transfer_assistant_stage }}/wheels"
        owner: root
        group: root
        mode: "0550"

    - name: Publish immutable assistant MCP wheelhouse atomically
      ansible.builtin.command:
        argv:
          - /usr/bin/mv
          - "{{ ai_ops_mcp_wheelhouse_transfer_assistant_stage }}"
          - "{{ ai_ops_mcp_wheelhouse_transfer_destination }}"
      changed_when: true

    - name: Seal published assistant MCP wheelhouse directory
      ansible.builtin.file:
        path: "{{ ai_ops_mcp_wheelhouse_transfer_destination }}"
        owner: root
        group: root
        mode: "0550"
  always:
    - name: Remove controller MCP wheelhouse staging path
      ansible.builtin.file:
        path: "{{ ai_ops_mcp_wheelhouse_transfer_controller_stage }}"
        state: absent
      delegate_to: localhost
      become: false

    - name: Remove assistant MCP wheelhouse staging path
      ansible.builtin.file:
        path: "{{ ai_ops_mcp_wheelhouse_transfer_assistant_stage }}"
        state: absent
