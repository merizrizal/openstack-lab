---
lab:
  vars:
    root_dir: "{{ lookup('ansible.builtin.env', 'ROOT_DIR') }}"
    target_env: local
    var_file_path: "{{ (root_dir | default(playbook_dir | dirname)) ~ '/inventories/' ~ (target_env | default('local')) ~ '/nodes.yml' }}"
  children:
    ceph_adm:
      hosts:
        ceph01:
          ansible_host: "{{ ceph01.mgmtnet_ip_address }}"
    # ceph_common:
    #   hosts:
    #     ceph02:
    #       ansible_host: "{{ ceph02.mgmtnet_ip_address }}"
    #     ceph03:
    #       ansible_host: "{{ ceph03.mgmtnet_ip_address }}"
