---
- name: Deploy CI monitor to CI/CD Lab
  hosts: ci_monitor*
  become: true
  vars:
    root_dir: "{{ lookup('ansible.builtin.env', 'ROOT_DIR') | default(playbook_dir | dirname | dirname, true) }}"
  roles:
    - role: telemetry
