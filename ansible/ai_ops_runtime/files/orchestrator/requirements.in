# Direct inputs for the fake-first orchestrator package and its local checks.
# Update only through the reviewed hash-lock procedure in pyproject.toml/ADS policy.
mcp==1.28.1
openai-codex==0.144.4
mypy==1.15.0
pytest==8.3.5
ruff==0.11.2
setuptools==75.8.0
